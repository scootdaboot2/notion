{
  "name": "time-tracker",
  "version": "1.0.0",
  "main": "src/TimeTracker.js",
  "dependencies": {
    "preact": "^10.5.13",
    "preact-router": "^4.0.1",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "date-fns": "^2.29.2"
  },
  "scripts": {
    "dev": "preact build && preact watch",
    "build": "preact build",
    "start": "preact serve"
  }
}
